<% if Monologue::Config.sidebar %>
    <aside class="sidebar four columns">
       <% Monologue::Config.sidebar.each do |ext| %>
         <%= render partial: "monologue/sidebar/#{ext}", locals: {post: post} %>
       <% end %>
    </aside>
<% end %>
